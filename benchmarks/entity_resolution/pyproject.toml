[project]
name = "entity_resolution_benchmark"
version = "0.1.0"
description = "Synthetic benchmark for Synor entity resolution"
requires-python = ">=3.11"
dependencies = [
    "synor[entity_resolution_llm]",
]

[tool.uv]
package = false

[tool.uv.sources]
synor = {path = "../../", editable = true}
