[project]
name = "state_store_bench"
version = "0.1.0"
description = "Latency benchmark for the synor state store (LMDB)"
requires-python = ">=3.11"
dependencies = [
    "synor",
]

[tool.uv]
package = false

[tool.uv.sources]
synor = {path = "../../", editable = true}
