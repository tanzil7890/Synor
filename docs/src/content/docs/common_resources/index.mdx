---
title: "*Common resources*"
description: >
  Shared types and utilities used across connectors and operations: data models,
  vector schema, stable ID generation, and the in-memory LiveMap.
---
import DocCardGrid from '/src/components/DocCardGrid.astro';
import { commonResourcesGroups } from '/src/data/section-grids';

The building blocks that connectors and operations share. Reach for these when you wire your own pieces together, or need the exact type a connector expects.

<DocCardGrid groups={commonResourcesGroups} ariaLabel="Common resources" />
