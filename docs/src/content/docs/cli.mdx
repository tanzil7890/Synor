---
title: The *CLI* reference
description: >
  Reference for the `synor` CLI — app target formats, environment variable
  loading, diagnosis, preview, explanation, controlled update, and cleanup.
---
Synor CLI is a standalone tool for easily managing and inspecting your apps.

## Invoke the CLI

Once Synor is installed, you can invoke the CLI directly using the `synor` command. Most commands require an `APP_TARGET` argument, which tells the CLI where your app definitions are located.

### APP_TARGET Format

The `APP_TARGET` can be:

1. A **Python module name** that contains your app definitions (e.g., `main`, `my_package.apps`).
    You can also use `--app-dir <path>` to specify the base directory to load the module from.

2. A **path to a Python file** defining your apps (e.g., `main.py`, `path/to/my_apps.py`).

    The file will be loaded as a top-level Python module, e.g. relative imports will not work as its parent package is not defined (similar to how `python main.py` resolves imports).

3. For commands that operate on a *specific app* (like `show`, `update`), you can combine the application reference with an app name:
    * `my_package.apps:MyApp`
    * `path/to/my_apps.py:MyApp`

### Environment Variables

You can set environment variables in an environment file.

* By default, the `synor` CLI searches upward from the current directory for a `.env` file.
* You can use `--env-file <path>` to specify one explicitly:

    ```sh
    synor --env-file path/to/custom.env <COMMAND> ...
    ```

Loaded variables do *NOT* override existing system ones.
If no file is found, only existing system environment variables are used.

### Global Options

Synor CLI supports the following global options:

* `-e, --env-file <path>`: Load environment variables from a specified `.env` file. If not provided, `.env` in the current directory is loaded if it exists.
* `-d, --app-dir <path>`: Load apps from the specified directory. It will be treated as part of `PYTHONPATH`. Default to the current directory.
* `--offline`: Deny standard Python network and DNS operations for the entire command.
* `-V, --version`: Show the Synor version and exit.
* `--help`: Show the main help message and exit.

`plan`, `diff`, `update`, `drop`, `doctor`, and `explain` also accept
`--offline` after the subcommand. See
[Review a run before it applies](./programming_guide/controlled_runs) for the
policy boundary, run-manifest format, and Python control API.

{/* BEGIN AUTO-GENERATED CLI SUBCOMMANDS — do not edit by hand. */}
{/* Regenerate with: python dev/generate_cli_docs.py */}

## Subcommands Reference

### `dashboard`

Serve the read-only local run dashboard.


**Usage:**

```bash
synor dashboard [OPTIONS]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--host TEXT` | [default: 127.0.0.1] |
| `--port INTEGER RANGE` | [default: 8765; 0&lt;=x&lt;=65535] |
| `--allow-remote` | Allow a non-loopback bind. The dashboard has no authentication. |
| `--open-browser` | Open the local dashboard URL in the default browser. |
| `--help` | Show this message and exit. |

---

### `diff`

Show a redacted target-action diff without applying it.


**Usage:**

```bash
synor diff [OPTIONS] APP_TARGET
```

**Options:**

| Option | Description |
|--------|-------------|
| `--full-reprocess` | Diff with all memoized work invalidated. |
| `--offline` | Deny guarded Python networking and policy-aware egress. |
| `--json` | Emit JSON. |
| `--help` | Show this message and exit. |

---

### `doctor`

Check whether this machine can run Synor safely.


**Usage:**

```bash
synor doctor [OPTIONS] [APP_TARGET]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--model PATH` | Validate that a local model file exists. |
| `--offline` | Run app checks with guarded networking denied. |
| `--json` | Emit JSON. |
| `--help` | Show this message and exit. |

---

### `drop`

Drop an app and all its target states.

This will:

- Revert all target states created by the app (e.g., drop tables, delete rows)
- Clear the app's internal state database

`APP_TARGET`: `path/to/app.py`, `module`, `path/to/app.py:app_name`, or
`module:app_name`.


**Usage:**

```bash
synor drop [OPTIONS] APP_TARGET
```

**Options:**

| Option | Description |
|--------|-------------|
| `-f, --force` | Skip confirmation prompt. |
| `-q, --quiet` | Avoid printing anything to the standard output, e.g. statistics. |
| `--offline` | Deny guarded Python networking and policy-aware egress. |
| `--help` | Show this message and exit. |

---

### `explain`

Explain an app's local state, ownership, policy, and latest run.


**Usage:**

```bash
synor explain [OPTIONS] APP_TARGET
```

**Options:**

| Option | Description |
|--------|-------------|
| `--offline` | Deny guarded Python networking and policy-aware egress. |
| `--json` | Emit JSON. |
| `--help` | Show this message and exit. |

---

### `init`

Initialize a new Synor project.

Creates a new project directory with starter files: 1. main.py (Main
application file) 2. pyproject.toml (Project metadata and dependencies) 3.
README.md (Quick start guide)

`PROJECT_NAME`: Name of the project (defaults to current directory name if
not specified).


**Usage:**

```bash
synor init [OPTIONS] [PROJECT_NAME]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--dir DIRECTORY` | Directory to create the project in. |
| `--help` | Show this message and exit. |

---

### `lock`

Create an offline-verifiable pipeline lockfile.


**Usage:**

```bash
synor lock [OPTIONS] APP_TARGET
```

**Options:**

| Option | Description |
|--------|-------------|
| `--output PATH` | [default: synor.lock.json] |
| `--help` | Show this message and exit. |

---

### `ls`

List all apps.

If `APP_TARGET` (`path/to/app.py` or `module`) is provided, lists apps
defined in that module and their persisted status, grouped by environment.

If `APP_TARGET` is omitted and `--db` is provided, lists all apps from the
specified database.


**Usage:**

```bash
synor ls [OPTIONS] [APP_TARGET]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--db TEXT` | Path to database to list apps from (only used when APP_TARGET is not specified). |
| `--help` | Show this message and exit. |

---

### `package`

Build a deterministic local pipeline package and lockfile.


**Usage:**

```bash
synor package [OPTIONS] APP_TARGET
```

**Options:**

| Option | Description |
|--------|-------------|
| `--output PATH` | [default: pipeline.synor] |
| `--lockfile PATH` | [default: synor.lock.json] |
| `--help` | Show this message and exit. |

---

### `package-verify`

Verify a Synor pipeline package without extracting or downloading.


**Usage:**

```bash
synor package-verify [OPTIONS] PACKAGE_PATH
```

**Options:**

| Option | Description |
|--------|-------------|
| `--help` | Show this message and exit. |

---

### `plan`

Show what an app would change without applying it.


**Usage:**

```bash
synor plan [OPTIONS] APP_TARGET
```

**Options:**

| Option | Description |
|--------|-------------|
| `--full-reprocess` | Plan with all memoized work invalidated. |
| `--offline` | Deny guarded Python networking and policy-aware egress. |
| `--json` | Emit JSON. |
| `--help` | Show this message and exit. |

---

### `quarantine`

Inspect and manually review quarantined failures.


**Usage:**

```bash
synor quarantine [OPTIONS] COMMAND [ARGS]...
```

**Options:**

| Option | Description |
|--------|-------------|
| `--help` | Show this message and exit. |

---

### `quarantine list`

List local quarantine cases.


**Usage:**

```bash
synor quarantine list [OPTIONS]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--status [open|approved|rejected]` |  |
| `--json` | Emit JSON. |
| `--help` | Show this message and exit. |

---

### `quarantine review`

Approve or reject a case without executing pipeline code.


**Usage:**

```bash
synor quarantine review [OPTIONS] CASE_ID
```

**Options:**

| Option | Description |
|--------|-------------|
| `--decision [approve|reject]` | [required] |
| `--note TEXT` | Optional redacted reviewer note. |
| `--help` | Show this message and exit. |

---

### `quarantine show`

Show one metadata-only quarantine case.


**Usage:**

```bash
synor quarantine show [OPTIONS] CASE_ID
```

**Options:**

| Option | Description |
|--------|-------------|
| `--help` | Show this message and exit. |

---

### `replay`

Re-run a captured preview and verify deterministic evidence.


**Usage:**

```bash
synor replay [OPTIONS] REPLAY_PATH
```

**Options:**

| Option | Description |
|--------|-------------|
| `--app-target TEXT` | Override the APP_TARGET captured in the replay envelope. |
| `--offline` | Deny guarded Python networking and policy-aware egress. |
| `--json` | Emit JSON. |
| `--help` | Show this message and exit. |

---

### `revocations`

Inspect and operate evidence-backed index revocations.


**Usage:**

```bash
synor revocations [OPTIONS] COMMAND [ARGS]...
```

**Options:**

| Option | Description |
|--------|-------------|
| `--help` | Show this message and exit. |

---

### `revocations list`

List redacted revocation case summaries.


**Usage:**

```bash
synor revocations list [OPTIONS]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--status [open|overdue|observed|suppressed_from_serving|deletion_planned|dispatched|acknowledged|consistency_fence_reached|absence_verified|retained_isolated|closed|failed|blocked]` | Filter by lifecycle stage, open cases, or overdue cases. |
| `--json` | Emit versioned JSON. |
| `--help` | Show this message and exit. |

---

### `revocations repair-ledger`

Rebuild derived ledger summaries from immutable events and receipts.


**Usage:**

```bash
synor revocations repair-ledger [OPTIONS]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--json` | Emit versioned JSON. |
| `--help` | Show this message and exit. |

---

### `revocations retry`

Explicitly retry target mutation and require a new durable receipt attempt.


**Usage:**

```bash
synor revocations retry [OPTIONS] CASE_ID
```

**Options:**

| Option | Description |
|--------|-------------|
| `--operator TEXT` | Explicit MODULE:OBJECT implementing RevocationOperator. |
| `--json` | Emit versioned JSON. |
| `--help` | Show this message and exit. |

---

### `revocations scan`

Request a read-only drift scan from a trusted target operator.


**Usage:**

```bash
synor revocations scan [OPTIONS]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--target TEXT` | [required] |
| `--operator TEXT` | Explicit MODULE:OBJECT implementing RevocationOperator. |
| `--json` | Emit versioned JSON. |
| `--help` | Show this message and exit. |

---

### `revocations show`

Show one redacted case and its receipt chain.


**Usage:**

```bash
synor revocations show [OPTIONS] CASE_ID
```

**Options:**

| Option | Description |
|--------|-------------|
| `--json` | Emit versioned JSON. |
| `--help` | Show this message and exit. |

---

### `revocations verify`

Request read-only destination verification from a trusted operator.


**Usage:**

```bash
synor revocations verify [OPTIONS] CASE_ID
```

**Options:**

| Option | Description |
|--------|-------------|
| `--operator TEXT` | Explicit MODULE:OBJECT implementing RevocationOperator. |
| `--json` | Emit versioned JSON. |
| `--help` | Show this message and exit. |

---

### `show`

Show the app's stable paths.

If `APP_TARGET` is provided, loads the app from the module.
Otherwise, `--db` and `--app-name` can be used to inspect an app
directly from its database without loading the module.


**Usage:**

```bash
synor show [OPTIONS] [APP_TARGET] [STABLE_PATH]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--db TEXT` | Path to database (used with --app-name when APP_TARGET is not specified). |
| `--app-name TEXT` | App name to inspect (used with --db when APP_TARGET is not specified). |
| `--tree` | Display stable paths (or --target-states entries) as a tree. |
| `-l, --long` | Display detailed information in multi-line format. |
| `-r, --recursive` | Show all children recursively (requires stable_path). |
| `-p, --parents` | Show all parent paths (requires stable_path). |
| `--fingerprints` | Show target-state paths as raw fingerprints (as stored) instead of readable keys. |
| `--target-states` | List all tracked target states with their owner components. |
| `--help` | Show this message and exit. |

---

### `state-key`

Generate an encryption key for SYNOR_STATE_KEY.


**Usage:**

```bash
synor state-key [OPTIONS]
```

**Options:**

| Option | Description |
|--------|-------------|
| `--help` | Show this message and exit. |

---

### `update`

Run an app in catch-up mode. With --live, run in live mode.

`APP_TARGET`: `path/to/app.py`, `module`, `path/to/app.py:app_name`, or
`module:app_name`.


**Usage:**

```bash
synor update [OPTIONS] APP_TARGET
```

**Options:**

| Option | Description |
|--------|-------------|
| `-f, --force` | Skip confirmation prompt. |
| `-q, --quiet` | Avoid printing anything to the standard output, e.g. statistics. |
| `--reset` | Drop existing setup before updating (equivalent to running 'synor drop' first). |
| `--full-reprocess` | Reprocess everything and invalidate existing caches. |
| `-L, --live` | Run in live mode (live components continue processing after initial update). |
| `--preview` | Compute target actions without applying them. Prints planned actions. |
| `--offline` | Deny guarded Python networking and policy-aware egress. |
| `--help` | Show this message and exit. |

---

{/* END AUTO-GENERATED CLI SUBCOMMANDS */}
