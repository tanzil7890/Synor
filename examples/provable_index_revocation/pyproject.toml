[project]
name = "provable-index-revocation"
version = "0.1.0"
description = "Synor flagship demo for query-verified AI-index revocation."
requires-python = ">=3.11"
dependencies = ["synor>=0.1.0a1"]

[project.optional-dependencies]
real = ["synor[google_drive,qdrant]>=0.1.0a1"]

[tool.setuptools]
packages = []
