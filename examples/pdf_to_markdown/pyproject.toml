[project]
name = "pdf-to-markdown-v1"
version = "0.1.0"
description = "Synor v1 example: convert PDF files to markdown using docling."
requires-python = ">=3.11"
dependencies = [
    "synor>=0.1.0a1",
    "docling>=2.0.0",
]

[tool.setuptools]
packages = []
