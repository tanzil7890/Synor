SYNOR_DB=./synor.db

# entire_session_search — environment template.
# Copy to .env (auto-loaded when you run the example): cp .env.example .env

# Postgres + pgvector target. Start a local one: docker compose -f ../../dev/postgres.yaml up -d
POSTGRES_URL=postgres://synor:synor@localhost/synor

# Optional — override where rows land (defaults are fine for a first run).
# PG_SCHEMA_NAME=synor_examples
# TABLE_EMBEDDINGS=entire_session_embeddings
# TABLE_METADATA=entire_session_metadata

# Fallback to CPU for operations not supported by MPS on Mac.
# It's no-op for other platforms.
PYTORCH_ENABLE_MPS_FALLBACK=1
