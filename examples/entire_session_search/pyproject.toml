[project]
name = "entire-session-search"
version = "0.1.0"
description = "Semantic search over AI coding sessions captured by Entire, powered by Synor."
requires-python = ">=3.11"
dependencies = [
    "synor[postgres,sentence_transformers]>=0.1.0a1",
    "asyncpg>=0.29.0",
    "pgvector>=0.4.1",
    "numpy",
    "python-dotenv>=1.0.1",
]

[tool.setuptools]
packages = []
