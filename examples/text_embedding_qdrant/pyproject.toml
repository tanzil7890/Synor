[project]
name = "text-embedding-qdrant-v1"
version = "0.1.0"
description = "Synor v1 example: embed markdown files and store in Qdrant."
requires-python = ">=3.11"
dependencies = [
    "synor[sentence_transformers,qdrant]>=0.1.0a1",
    "numpy",
    "qdrant-client>=1.16.0",
    "python-dotenv>=1.0.1",
]

[tool.setuptools]
packages = []
