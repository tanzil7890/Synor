SYNOR_DB=./synor.db

# multi_codebase_summarization — environment template.
# Copy to .env (auto-loaded when you run the example): cp .env.example .env

# LLM key — default model is gemini/gemini-2.5-flash (via instructor/litellm).
GEMINI_API_KEY=
# Optional: switch the model (then set the matching provider key).
# LLM_MODEL=gemini/gemini-2.5-flash
