[workspace]

[package]
name = "amazon_s3_embedding"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"

[dependencies]
# Amazon S3 source + Postgres/pgvector target both live in the SDK. The `text`
# + `fastembed` features pull in the SDK ops facades (RecursiveSplitter and the
# SentenceTransformerEmbedder) so this example no longer hand-rolls chunking or
# the fastembed plumbing.
synor = { path = "../../../rust/sdk/synor", features = [
    "postgres",
    "amazon_s3",
    "text",
    "fastembed",
] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
# Query path (pgvector similarity); target writes go through synor::postgres.
sqlx = { version = "0.8", default-features = false, features = [
    "runtime-tokio",
    "tls-rustls",
    "postgres",
] }
dotenvy = "0.15"
