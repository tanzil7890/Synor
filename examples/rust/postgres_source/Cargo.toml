[workspace]

[package]
name = "postgres_source"
version = "0.1.0"
edition = "2024"

[dependencies]
synor = { path = "../../../rust/sdk/synor", features = ["postgres", "fastembed"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
# Query path (pgvector similarity); target writes go through synor::postgres.
sqlx = { version = "0.8", default-features = false, features = [
    "runtime-tokio",
    "tls-rustls",
    "postgres",
] }
dotenvy = "0.15"
