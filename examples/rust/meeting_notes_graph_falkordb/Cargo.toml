[workspace]

[package]
name = "meeting_notes_graph_falkordb"
version = "0.1.0"
edition = "2024"

[dependencies]
synor = { path = "../../../rust/sdk/synor", features = ["falkordb"] }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
chrono = { version = "0.4", default-features = false, features = ["std"] }
dotenvy = "0.15"
