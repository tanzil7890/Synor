[workspace]

[package]
name = "pdf_to_markdown"
version = "0.1.0"
edition = "2024"

[dependencies]
synor = { path = "../../../rust/sdk/synor" }
tokio = { version = "1", features = ["full"] }
# PDF text extraction (Rust-native stand-in for Python's docling converter).
lopdf = "0.40"
