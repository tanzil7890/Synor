[project]
name = "code-embedding-lancedb-v1"
version = "0.1.0"
description = "Synor v1 example: embed code files and store in LanceDB."
requires-python = ">=3.11"
dependencies = [
    "synor[sentence_transformers,lancedb]>=0.1.0a1",
    "python-dotenv>=1.0.1",
]

[tool.setuptools]
packages = []
