[project]
name = "files-transform"
version = "0.1.0"
description = "Simple example for synor: transform files in a directory."
requires-python = ">=3.11"
dependencies = ["synor>=0.1.0a1", "markdown-it-py[linkify,plugins]"]

[tool.setuptools]
packages = []
