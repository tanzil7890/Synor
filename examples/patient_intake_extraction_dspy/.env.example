SYNOR_DB=./synor.db

# patient_intake_extraction_dspy — environment template.
# Copy to .env (auto-loaded when you run the example): cp .env.example .env

# Gemini API key — the extraction uses a Gemini vision model.
GEMINI_API_KEY=
