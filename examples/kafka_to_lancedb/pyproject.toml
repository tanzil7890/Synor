[project]
name = "kafka-to-lancedb"
version = "0.1.0"
description = "Synor example: consume Kafka messages and dispatch to LanceDB tables."
requires-python = ">=3.11"
dependencies = ["synor[kafka,lancedb]>=0.1.0a1"]

[tool.setuptools]
packages = []
