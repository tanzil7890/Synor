[project]
name = "audio-to-text-v1"
version = "0.1.0"
description = "Synor v1 example: transcribe audio files to text using LiteLLM."
requires-python = ">=3.11"
dependencies = [
    "synor[litellm,postgres]>=0.1.0a1",
    "asyncpg>=0.29.0",
]

[tool.setuptools]
packages = []
