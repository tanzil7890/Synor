[project]
name = "index-integrity-local"
version = "0.1.0"
description = "Service-free read-only Synor integrity scan."
requires-python = ">=3.11"
dependencies = ["synor>=0.1.0a1"]

[tool.setuptools]
packages = []
