[project]
name = "csv-to-kafka"
version = "0.1.0"
description = "Synor example: watch CSV files and publish rows as JSON to Kafka."
requires-python = ">=3.11"
dependencies = ["synor[kafka]>=0.1.0a1"]

[tool.setuptools]
packages = []
